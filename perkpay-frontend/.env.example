# API base URL for the backend.
#
# Local dev:   VITE_API_BASE=http://localhost:4000
# Vercel:      Leave EMPTY (frontend and backend share the same domain).
#              The code defaults to "" which means same-origin fetch.
VITE_API_BASE=http://localhost:4000
